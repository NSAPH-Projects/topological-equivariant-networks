experiment_name: "11"
description: "EGNN-like, no VC"

# Dataset args
lifters: ["atom:0", "bond:1"]
neighbor_types: "max"
connectivity: "self"
visible_dims: [0]
initial_features: "hetero"
dim: 1

# Model args
num_hidden: 128