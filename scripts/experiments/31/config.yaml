experiment_name: "31"
description: "isolated ranks: atoms + bonds. no hetero features. hetero neighbors"

# Dataset args
lifters: ["atom:0", "bond:1", "supercell:2"]
neighbor_types: ["+1", "-1", "max"]
connectivity: "self"
visible_dims: [0, 1]
initial_features: "node"
dim: 2

# Model args
num_hidden: 128