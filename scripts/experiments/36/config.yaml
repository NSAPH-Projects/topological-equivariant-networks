experiment_name: "36"
description: "interrank: atoms + bonds + (functional groups + rings). no VC. self + neighbors. hetero neighbors"

# Dataset args
lifters: ["atom:0", "bond:1", "ring:2", "functional_group:2"]
neighbor_types: ["+1", "-1", "max"]
connectivity: "self_and_neighbors"
visible_dims: [0, 1, 2]
initial_features: "hetero"
dim: 2

# Model args
num_hidden: 70