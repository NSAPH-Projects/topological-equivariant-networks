experiment_name: "experiment_ring_test"
description: "interrank: atoms + bonds + rings"

# Dataset args
lifters: ["atom:0", "bond:1", "ring:2"]
neighbor_types: "max"
connectivity: "all_to_all"
visible_dims: [0, 1, 2]
initial_features: "hetero"
dim: 2
triangles_only: true

# Model args
num_hidden: 100
use_geometry: false