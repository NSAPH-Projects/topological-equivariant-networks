experiment_name: "32"
description: "interrank: atoms + bonds. self + neighbors. hetero neighbors"

# Dataset args
lifters: ["atom:0", "bond:1", "supercell:2"]
neighbor_types: ["+1", "-1", "max"]
connectivity: "self_and_neighbors"
visible_dims: [0, 1]
initial_features: "hetero"
dim: 2

# Model args
num_hidden: 100